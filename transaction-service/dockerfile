FROM golang:latest

WORKDIR /app

ENV POSTGRE_HOST=
ENV POSTGRE_USER=
ENV POSTGRE_PASSWORD=
ENV POSTGRE_DB=
ENV POSTGRE_PORT=
ENV APPS_PORT=8082

COPY go.mod .
COPY go.sum .

RUN go mod download

COPY . .

RUN go build -o main .

EXPOSE $APPS_PORT

CMD ["./main"]